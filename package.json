{
  "name": "crypto-news-publisher",
  "version": "1.0.0",
  "description": "Automated crypto news publisher with Gemini and Telegram.",
  "main": "server/index.js",
  "private": true,
  "scripts": {
    "start": "node server/index.js",
    "dev": "echo 'Run the app using the Docker instructions in README.md'",
    "build": "mkdir -p public && esbuild index.tsx --bundle --outfile=public/bundle.js --loader:.tsx=tsx --jsx=automatic"
  },
  "dependencies": {
    "@google/genai": "^1.29.0",
    "body-parser": "^1.20.2",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "node-fetch": "^3.3.2"
  },
  "devDependencies": {
    "esbuild": "^0.21.4"
  }
}